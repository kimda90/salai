{
  "name": "salai",
  "private": true,
  "scripts": {
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test",
    "test:browser": "pnpm --filter @salai/spike-demo test:browser",
    "typecheck": "pnpm --recursive typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
